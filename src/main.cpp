#include <iostream>
#include <string>
#include "ApplicationManager.h"
#include "UI.h"

int main(int argc, char** argv) {
    /* 
    * Only call a manager and pass flags there.
    * 
    * Maybe I should include here framework 
    * implementation and pass it to manager through 
    * interface.
    *
    */
    // TODO: wxIMPLEMENT_APP_NO_MAIN
    // return ApplicationManager::Run(argc, argv, nullptr);
    std::cout << "For now this is just a stub." << std::endl;
    return 0;
}
