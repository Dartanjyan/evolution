#ifndef BRAIN_MUTATOR_H
#define BRAIN_MUTATOR_H
/*
#include "Brain.h"
#include <random>

/// Use-case: Works on Brain class objects and mutates them
class BrainMutator {
public:
    BrainMutator(double mutation_rate, double mutation_strength);

    // Одномоментная мутация одного мозга
    void mutate(Brain& brain);

    // Кроссовер двух родителей в одного потомка
    Brain* crossover(const Brain& parent1, const Brain& parent2);

private:
    double mutation_rate_;
    double mutation_strength_;
    std::mt19937 rng_;
};
*/
#endif // BRAIN_MUTATOR_H
